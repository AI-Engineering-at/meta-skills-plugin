---
description: "Full quality snapshot — harden + CI + behavioral tests in one view"
argument-hint: "[--quick] — quick: skip behavioral tests (faster)"
---

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/scripts/quality-snapshot.py" $ARGUMENTS
```
